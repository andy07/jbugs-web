import { ButtonDisableDirective } from './button-disable.directive';

describe('ButtonDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDisableDirective();
    expect(directive).toBeTruthy();
  });
});
